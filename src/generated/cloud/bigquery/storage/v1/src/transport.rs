// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

#[allow(unused_imports)]
use crate::Error;
use crate::Result;

/// Implements [BigQueryRead](super::stub::BigQueryRead) using a [gaxi::http::ReqwestClient].
#[derive(Clone)]
pub struct BigQueryRead {
    inner: gaxi::http::ReqwestClient,
}

impl std::fmt::Debug for BigQueryRead {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::result::Result<(), std::fmt::Error> {
        f.debug_struct("BigQueryRead")
            .field("inner", &self.inner)
            .finish()
    }
}

impl BigQueryRead {
    pub async fn new(config: gaxi::options::ClientConfig) -> crate::ClientBuilderResult<Self> {
        let tracing_is_enabled = gaxi::options::tracing_enabled(&config);
        let inner = gaxi::http::ReqwestClient::new(config, crate::DEFAULT_HOST).await?;
        let inner = if tracing_is_enabled {
            inner.with_instrumentation(&super::tracing::info::INSTRUMENTATION_CLIENT_INFO)
        } else {
            inner
        };
        Ok(Self { inner })
    }
}

impl super::stub::BigQueryRead for BigQueryRead {
    async fn create_read_session(
        &self,
        req: crate::model::CreateReadSessionRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::ReadSession>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_read_session_table = try_match(
                    Some(&req)
                        .and_then(|m| m.read_session.as_ref())
                        .map(|m| &m.table)
                        .map(|s| s.as_str()),
                    &[
                        Segment::Literal("projects/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/datasets/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/tables/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_read_session_table,);
                let path_template = "/v1/{read_session.table}";

                let resource_name = format!(
                    "//bigquerystorage.googleapis.com/{}",
                    var_read_session_table,
                );
                let builder = self.inner.builder(Method::POST, path);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req)
                            .and_then(|m| m.read_session.as_ref())
                            .map(|m| &m.table)
                            .map(|s| s.as_str()),
                        &[
                            Segment::Literal("projects/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/datasets/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/tables/"),
                            Segment::SingleWildcard,
                        ],
                        "read_session.table",
                        "projects/*/datasets/*/tables/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method(
                        "google.cloud.bigquery.storage.v1.BigQueryRead/CreateReadSession",
                    )
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(Some(req), &method);
        self.inner.execute(builder, body, options).await
    }

    async fn split_read_stream(
        &self,
        req: crate::model::SplitReadStreamRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::SplitReadStreamResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("projects/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/locations/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/sessions/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/streams/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_name,);
                let path_template = "/v1/{name}";

                let resource_name = format!("//bigquerystorage.googleapis.com/{}", var_name,);
                let builder = self.inner.builder(Method::GET, path);
                let builder = builder.query(&[("fraction", &req.fraction)]);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::GET, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("projects/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/locations/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/sessions/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/streams/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "projects/*/locations/*/sessions/*/streams/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("google.cloud.bigquery.storage.v1.BigQueryRead/SplitReadStream")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(None::<gaxi::http::NoBody>, &method);
        self.inner.execute(builder, body, options).await
    }
}

/// Implements [BigQueryWrite](super::stub::BigQueryWrite) using a [gaxi::http::ReqwestClient].
#[derive(Clone)]
pub struct BigQueryWrite {
    inner: gaxi::http::ReqwestClient,
    #[cfg(google_cloud_unstable_gapic_streaming)]
    grpc_inner: gaxi::grpc::Client,
}

impl std::fmt::Debug for BigQueryWrite {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::result::Result<(), std::fmt::Error> {
        let mut builder = f.debug_struct("BigQueryWrite");
        builder.field("inner", &self.inner);
        #[cfg(google_cloud_unstable_gapic_streaming)]
        builder.field("grpc_inner", &self.grpc_inner);
        builder.finish()
    }
}

impl BigQueryWrite {
    pub async fn new(config: gaxi::options::ClientConfig) -> crate::ClientBuilderResult<Self> {
        let tracing_is_enabled = gaxi::options::tracing_enabled(&config);
        let inner = gaxi::http::ReqwestClient::new(config.clone(), crate::DEFAULT_HOST).await?;
        let inner = if tracing_is_enabled {
            inner.with_instrumentation(&super::tracing::info::INSTRUMENTATION_CLIENT_INFO)
        } else {
            inner
        };
        #[cfg(google_cloud_unstable_gapic_streaming)]
        let grpc_inner = if tracing_is_enabled {
            gaxi::grpc::Client::new_with_instrumentation(
                config,
                crate::DEFAULT_HOST,
                &super::tracing::info::INSTRUMENTATION_CLIENT_INFO,
            )
            .await?
        } else {
            gaxi::grpc::Client::new(config, crate::DEFAULT_HOST).await?
        };
        Ok(Self {
            inner,
            #[cfg(google_cloud_unstable_gapic_streaming)]
            grpc_inner,
        })
    }
}

impl super::stub::BigQueryWrite for BigQueryWrite {
    async fn create_write_stream(
        &self,
        req: crate::model::CreateWriteStreamRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::WriteStream>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_parent = try_match(
                    Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                    &[
                        Segment::Literal("projects/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/datasets/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/tables/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_parent,);
                let path_template = "/v1/{parent}";

                let resource_name = format!("//bigquerystorage.googleapis.com/{}", var_parent,);
                let builder = self.inner.builder(Method::POST, path);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                        &[
                            Segment::Literal("projects/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/datasets/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/tables/"),
                            Segment::SingleWildcard,
                        ],
                        "parent",
                        "projects/*/datasets/*/tables/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method(
                        "google.cloud.bigquery.storage.v1.BigQueryWrite/CreateWriteStream",
                    )
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(req.write_stream, &method);
        self.inner.execute(builder, body, options).await
    }

    #[cfg(google_cloud_unstable_gapic_streaming)]
    async fn append_rows(
        &self,
        req: std::option::Option<crate::model::AppendRowsRequest>,
        options: crate::BidiStreamOptions,
    ) -> Result<(
        google_cloud_gax::streaming::RequestSender<crate::model::AppendRowsRequest>,
        google_cloud_gax::streaming::ResponseReceiver<crate::model::AppendRowsResponse>,
    )> {
        use futures::stream::StreamExt as _;
        use gaxi::prost::{FromProto, ToProto};

        let req =
            req.ok_or_else(|| google_cloud_gax::error::Error::binding("a request is required"))?;

        let x_goog_request_params = "";

        let first_req = req
            .to_proto()
            .map_err(google_cloud_gax::error::Error::ser)?;

        let (req_tx, req_rx) = tokio::sync::mpsc::channel(options.request_channel_capacity());

        let req_stream = futures::stream::once(async move { first_req })
            .chain(tokio_stream::wrappers::ReceiverStream::new(req_rx));

        let extensions = {
            let mut e = gaxi::grpc::tonic::Extensions::new();
            e.insert(gaxi::grpc::tonic::GrpcMethod::new(
                "google.cloud.bigquery.storage.v1.BigQueryWrite",
                "AppendRows",
            ));
            e
        };
        let path = http::uri::PathAndQuery::from_static(
            "/google.cloud.bigquery.storage.v1.BigQueryWrite/AppendRows",
        );

        let result = self.grpc_inner
            .bidi_stream::<
                crate::prost::google::cloud::bigquery::storage::v1::AppendRowsRequest,
                crate::prost::google::cloud::bigquery::storage::v1::AppendRowsResponse,
            >(
                extensions,
                path,
                req_stream,
                options.into(),
                &crate::info::X_GOOG_API_CLIENT_HEADER,
                x_goog_request_params,
            )
            .await?;

        let request_sender = google_cloud_gax::streaming::RequestSender::from_fn(
            move |item: crate::model::AppendRowsRequest| {
                let req_tx = req_tx.clone();
                async move {
                    let prost_item = item
                        .to_proto()
                        .map_err(google_cloud_gax::error::Error::ser)?;
                    req_tx.send(prost_item).await.map_err(|_| {
                        google_cloud_gax::error::Error::io(std::io::Error::new(
                            std::io::ErrorKind::BrokenPipe,
                            "cannot send request: stream is closed",
                        ))
                    })
                }
            },
        );
        let response_receiver = google_cloud_gax::streaming::ResponseReceiver::from_stream(
            result.into_inner().map(|res| {
                res.map_err(gaxi::grpc::from_status::to_gax_error)
                    .and_then(|m| m.cnv().map_err(google_cloud_gax::error::Error::deser))
            }),
        );

        Ok((request_sender, response_receiver))
    }

    async fn get_write_stream(
        &self,
        req: crate::model::GetWriteStreamRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::WriteStream>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("projects/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/datasets/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/tables/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/streams/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_name,);
                let path_template = "/v1/{name}";

                let resource_name = format!("//bigquerystorage.googleapis.com/{}", var_name,);
                let builder = self.inner.builder(Method::POST, path);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("projects/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/datasets/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/tables/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/streams/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "projects/*/datasets/*/tables/*/streams/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("google.cloud.bigquery.storage.v1.BigQueryWrite/GetWriteStream")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(Some(req), &method);
        self.inner.execute(builder, body, options).await
    }

    async fn finalize_write_stream(
        &self,
        req: crate::model::FinalizeWriteStreamRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::FinalizeWriteStreamResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_name = try_match(
                    Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                    &[
                        Segment::Literal("projects/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/datasets/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/tables/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/streams/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_name,);
                let path_template = "/v1/{name}";

                let resource_name = format!("//bigquerystorage.googleapis.com/{}", var_name,);
                let builder = self.inner.builder(Method::POST, path);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.name).map(|s| s.as_str()),
                        &[
                            Segment::Literal("projects/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/datasets/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/tables/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/streams/"),
                            Segment::SingleWildcard,
                        ],
                        "name",
                        "projects/*/datasets/*/tables/*/streams/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method(
                        "google.cloud.bigquery.storage.v1.BigQueryWrite/FinalizeWriteStream",
                    )
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(Some(req), &method);
        self.inner.execute(builder, body, options).await
    }

    async fn batch_commit_write_streams(
        &self,
        req: crate::model::BatchCommitWriteStreamsRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::BatchCommitWriteStreamsResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_parent = try_match(
                    Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                    &[
                        Segment::Literal("projects/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/datasets/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/tables/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_parent,);
                let path_template = "/v1/{parent}";

                let resource_name = format!("//bigquerystorage.googleapis.com/{}", var_parent,);
                let builder = self.inner.builder(Method::GET, path);
                let builder = req
                    .write_streams
                    .iter()
                    .fold(builder, |builder, p| builder.query(&[("writeStreams", p)]));
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::GET, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.parent).map(|s| s.as_str()),
                        &[
                            Segment::Literal("projects/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/datasets/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/tables/"),
                            Segment::SingleWildcard,
                        ],
                        "parent",
                        "projects/*/datasets/*/tables/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method(
                        "google.cloud.bigquery.storage.v1.BigQueryWrite/BatchCommitWriteStreams",
                    )
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(None::<gaxi::http::NoBody>, &method);
        self.inner.execute(builder, body, options).await
    }

    async fn flush_rows(
        &self,
        req: crate::model::FlushRowsRequest,
        options: crate::RequestOptions,
    ) -> Result<crate::Response<crate::model::FlushRowsResponse>> {
        use gaxi::http::reqwest::{HeaderValue, Method};
        use gaxi::path_parameter::PathMismatchBuilder;
        use gaxi::path_parameter::try_match;
        use gaxi::routing_parameter::Segment;
        use google_cloud_gax::error::binding::BindingError;
        let (builder, method, _path_template, _resource_name) = None
            .or_else(|| {
                let var_write_stream = try_match(
                    Some(&req).map(|m| &m.write_stream).map(|s| s.as_str()),
                    &[
                        Segment::Literal("projects/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/datasets/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/tables/"),
                        Segment::SingleWildcard,
                        Segment::Literal("/streams/"),
                        Segment::SingleWildcard,
                    ],
                )?;
                let path = format!("/v1/{}", var_write_stream,);
                let path_template = "/v1/{write_stream}";

                let resource_name =
                    format!("//bigquerystorage.googleapis.com/{}", var_write_stream,);
                let builder = self.inner.builder(Method::POST, path);
                let builder = Ok(builder);
                Some(builder.map(|b| (b, Method::POST, path_template, resource_name)))
            })
            .ok_or_else(|| {
                let mut paths = Vec::new();
                {
                    let builder = PathMismatchBuilder::default();
                    let builder = builder.maybe_add(
                        Some(&req).map(|m| &m.write_stream).map(|s| s.as_str()),
                        &[
                            Segment::Literal("projects/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/datasets/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/tables/"),
                            Segment::SingleWildcard,
                            Segment::Literal("/streams/"),
                            Segment::SingleWildcard,
                        ],
                        "write_stream",
                        "projects/*/datasets/*/tables/*/streams/*",
                    );
                    paths.push(builder.build());
                }
                google_cloud_gax::error::Error::binding(BindingError { paths })
            })??;
        if let Some(recorder) = gaxi::observability::RequestRecorder::current() {
            recorder.on_client_request(
                gaxi::observability::ClientRequestAttributes::default()
                    .set_rpc_method("google.cloud.bigquery.storage.v1.BigQueryWrite/FlushRows")
                    .set_url_template(_path_template)
                    .set_resource_name(_resource_name),
            );
        }
        let options = google_cloud_gax::options::internal::set_default_idempotency(
            options,
            gaxi::http::default_idempotency(&method),
        );
        let builder = builder.query(&[("$alt", "json;enum-encoding=int")]).header(
            "x-goog-api-client",
            HeaderValue::from_static(&crate::info::X_GOOG_API_CLIENT_HEADER),
        );
        let body = gaxi::http::handle_empty(Some(req), &method);
        self.inner.execute(builder, body, options).await
    }
}
